const app=require('./app')
const PORT=4000

app.listen(PORT,()=>{
    console.log(`server has started at http://localhost:${PORT}`)
})



// console.log("hello ji oke twsted nodemon directes start and does it thing start from main entry points" )